{
  "name": "repo-map-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Fly.io worker: scans a repo, builds a code graph + embeddings in Neon/pgvector, serves repo_map queries for madmcp",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.12.0",
    "pgvector": "^0.2.0",
    "simple-git": "^3.25.0",
    "web-tree-sitter": "^0.22.6",
    "openai": "^4.60.0",
    "dotenv": "^16.4.5"
  }
}
